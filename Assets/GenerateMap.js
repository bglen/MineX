#pragma strict

var size;
var totalSkyboxes;

//function generateMap(size){
	//draw random number, 1 to totalSyboxes
	//select case and apply skybox to main camera
	
	//generate planets based on size variable
	
	//generate random integer numbers for x , y
	//check if they are too close to planets
	//check if they collide with another asteroid via asteroid array
	//if true, regenerate numbers
	//else, draw asteroids, add to asteroid array
	
	//space stations and other stuff
//}
function Start () {
	//set size to player's input
	//generateMap(size);
}

function Update () {

}